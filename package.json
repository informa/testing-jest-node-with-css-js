{
  "name": "lerna-jest-tests",
  "author": "JP",
  "private": true,
  "version": "0.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna exec --scope=library-vanilla-extract --scope=library-treat -- npm run build",
    "start:treat": "lerna exec --scope=app-cra4-rewired -- npm run start",
    "start:vanilla": "lerna exec --scope=app-cra5-craco -- npm run start",
    "test": "lerna exec --scope=app-cra4-rewired --scope=app-cra5-craco -- npm run test"
  },
  "devDependencies": {
    "lerna": "^6.6.2",
    "rimraf": "^4.1.2"
  }
}
