import { style } from "treat";

const styles = style({
  backgroundColor: "#007DB8",
  padding: "8px 12px",
  borderWidth: 0,
  borderRadius: "4px",
  color: "white",
  cursor: "pointer",
});

export default styles;
